/**
 * LookupMasterDTO is a Data Transfer Object (DTO) that represents the master data for lookups, including the id, type, key name, and key value.
 */
export class LookupMasterDTO {
	id: number = 0;
	type: string = "";
	keyName: string = "";
	keyValue: string = "";
}
