import { makeStyles } from "@fluentui/react-components";

const useStyles = makeStyles({
    noiseOverlay: {
        position: "absolute",
        left: 0,
        top: 0,
        width: "100vw",
        height: "100vh",
    },
});

export { useStyles };
